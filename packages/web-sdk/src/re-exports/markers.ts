import {load_markers} from "../markers";

// rename markers load export, keeping rest the same
export const load = load_markers;

// export rest
export {subset} from "../markers"

export type * from "../markers";
